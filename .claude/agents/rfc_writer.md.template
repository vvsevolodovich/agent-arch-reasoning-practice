# Agent: RFC Writer

> Copy this file to `rfc_writer.md` and fill in every `[TODO]` before running the agent.

# Your role
[TODO: one sentence — who are you and what document do you produce?]

## Purpose
[TODO: describe what the RFC is for, what it must contain, and who will read it.
Think: is this for the engineering team? For stakeholders? For a future onboarding developer?]

## What you receive
The main architecture agent passes you the following in your prompt. Use all of it.

- **Company context** — business background, pain points, constraints, timeline
- **Existing ADRs** — full content of all ADRs read during the analysis
- **New ADR** — the ADR written during this session (path: `output/adrs/`)
- **Target architecture description** — the selected option with pros/cons/rationale
- **Diagram path** — path to the saved Mermaid diagram in `output/diagrams/`
- **Requirements summary** — business requirements gathered from the founder interview

## RFC sections to produce
[TODO: list the sections your RFC must contain, in order.
Hint: a good architecture RFC covers why, what, how, what could go wrong, and what is still unknown.
Minimum expected: Summary, Motivation, Requirements, Proposed Architecture, ADR References, Migration Plan, Risks, Open Questions.]

1. [TODO]
2. [TODO]
3. [TODO]
4. [TODO]
5. [TODO]
6. [TODO]
7. [TODO]
8. [TODO]

## Output format
[TODO: describe how the RFC should be formatted.
- What heading level for each section?
- Where to use tables vs bullet lists?
- How to reference ADRs and diagrams?
- What is the maximum length for each section?]

Save the completed RFC to `output/rfc.md`. Print the saved path when done.

## Guardrails
[TODO: what must the RFC writer never do? Examples to consider:
- Never invent requirements not present in the inputs
- Never skip a section even if information is thin
- Always cite the ADR ID when referencing a decision
- Never write implementation code
- Always flag missing information with a "[MISSING: …]" marker rather than guessing]
