# Agent: [TODO: agent name]

> Copy this file to `CLAUDE.md` and fill in every `[TODO]` before running.

# Your role
[TODO: one sentence — who are you and what is your job in this project?]

## Purpose
[TODO: what does this agent do end-to-end?
Describe the full scope: what it reads, what it analyses, what it produces, and when it stops and waits.]

## Trigger
[TODO: what user input starts the workflow?
List the exact phrases or patterns that should trigger the full analysis.]

## Tools you can use

### Standalone skills

- `python skills/list_adrs/list_adrs.py`
  [TODO: when do you call this? What do you do with the result?]

- `python skills/read_adr/read_adr.py <ADR-ID>`
  [TODO: when do you call this? How many times? What are you looking for?]

- `python skills/write_adr/write_adr.py <ADR-ID> "<Title>" <content-file>`
  [TODO: when is it allowed to call this? What gate protects it?]

- `python skills/save_diagram/save_diagram.py "<diagram-name>" <content-file>`
  [TODO: when do you call this? What format must the content file be in?]

### Subagents

- **team_lead** — `subagent_type: "team_lead"`
  [TODO: when do you invoke this subagent? What do you pass to it? What do you do with the response?]

- **rfc_writer** — `subagent_type: "rfc_writer"`
  [TODO: when do you invoke this subagent? What context do you pass? Where does its output go?
  Note: check that `.claude/agents/rfc_writer.md` exists before invoking. If missing, skip and tell the user how to create it.]

### Optional tools (if available in your runtime)
- Read — for reading data files and draft files
- Write — for writing draft files to /tmp/ before passing to skills
- Bash — for running skill scripts

## Workflow (follow in order)

[TODO: write numbered steps for the full agent workflow.
Your workflow must include at minimum:
  1. Load company context
  2. List and read all ADRs
  3. Read current architecture diagram
  4. Clarify with founder subagent (when needed)
  5. Identify and present architectural gaps
  6. Propose and present solution options — then WAIT
  7. Generate diagram after user selects an option
  8. Write ADR (gated)
  9. Generate RFC via rfc_writer subagent (guarded)
  10. Present final summary
]

1. [TODO]
2. [TODO]
3. [TODO]
4. [TODO]
5. [TODO]
6. [TODO]
7. [TODO]
8. [TODO]
9. [TODO]
10. [TODO]

## Output format

[TODO: show the structure of the analysis you present to the user.
Include: gap analysis table, option descriptions, recommendation.
The user must see costs, risks, and timelines before making a decision.]

## Guardrails

[TODO: list the rules the agent must follow.
Required guardrails:
  - ADR write gate: never call write_adr without the user explicitly saying "write adr", "save adr", or "publish"
  - Diagram gate: never call save_diagram before user selects an option
  - RFC guard: check rfc_writer.md exists before invoking; skip gracefully if not
  - Read all ADRs before making recommendations
  - Respect team size (7 people) and budget (≤ €5k/month infra) constraints
]

## Failure handling

[TODO: what does the agent do when each tool fails?
Cover: list_adrs empty, read_adr file not found, team_lead unavailable, save_diagram fails, write_adr validation error, rfc_writer missing or failing, company.md missing.]
